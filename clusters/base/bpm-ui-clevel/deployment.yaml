apiVersion: apps/v1
kind: Deployment
metadata:
  name: bpm-ui-clevel
spec:
  replicas: 1
  selector:
    matchLabels:
      app: bpm-ui-clevel
  template:
    metadata:
      labels:
        app: bpm-ui-clevel
    spec:
      containers:
        - name: bpm-ui-clevel
          image: cr.yandex/bpm-registry/bpm-ui-clevel:placeholder
          ports:
            - containerPort: 4173
          readinessProbe:
            httpGet:
              path: /
              port: 4173
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: 4173
            initialDelaySeconds: 15
            periodSeconds: 20
