apiVersion: apps/v1
kind: Deployment
metadata:
  name: bpm-ui-analyst
spec:
  replicas: 1
  selector:
    matchLabels:
      app: bpm-ui-analyst
  template:
    metadata:
      labels:
        app: bpm-ui-analyst
    spec:
      containers:
        - name: bpm-ui-analyst
          image: cr.yandex/bpm-registry/bpm-ui-analyst:placeholder
          ports:
            - containerPort: 4173
          readinessProbe:
            httpGet:
              path: /
              port: 4173
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: 4173
            initialDelaySeconds: 15
            periodSeconds: 20
