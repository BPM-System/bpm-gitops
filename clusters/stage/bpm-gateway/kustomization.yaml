apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: bpm-stage
resources:
  - ../../base/bpm-gateway
images:
  - name: cr.yandex/bpm-registry/bpm-gateway
    newTag: stage
configMapGenerator:
  - name: bpm-gateway-config
    literals:
      - LOG_LEVEL=info
